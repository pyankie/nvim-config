return {
  {
    "iruzo/matrix-nvim",
    lazy = false,
    priority = 1000,
    config = function()
      -- vim.g.matrix_theme_variant = "dark" -- or "light" if available
      -- vim.cmd("colorscheme matrix")
    end,
  },
}
